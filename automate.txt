You are a senior software architect, senior full-stack engineer, professional SaaS product designer, PostgreSQL database architect, Go backend engineer, React frontend engineer, DevOps engineer, QA engineer, and automation engineer. Build a production-quality Real Estate Automation Platform for my real-estate business. The system must automate my property-processing workflow after I manually collect Facebook property post URLs. I will manually search Facebook for interesting properties, copy each Facebook post URL, and enter the URL into this application; do not build Facebook property discovery as part of the initial system. After I save a Facebook URL, the application must create a property record and provide a complete workflow: manually collected Facebook URL → property inbox → import/source content and images → property data extraction/entry → AI content generation → AI image enhancement → watermark → automatically select project/condo images → create final social-media assets → validate property → human review → approval → publishing queue → Facebook publishing → TikTok publishing → Google Sheets synchronization → publishing/status tracking. Use React.js with TypeScript and Vite for the frontend, Go with Fiber for the backend REST API, PostgreSQL with GORM for the database, Redis with Asynq or an equivalent Go background-job system for asynchronous processing, Docker and Docker Compose for the complete development and production environment, and S3-compatible object storage such as Cloudflare R2 for images and other files. The entire project must run through Docker Desktop, and the developer must be able to start the project using Docker Compose from a local desktop environment. Create frontend, backend API, background worker, PostgreSQL, Redis, and where appropriate Nginx services. Provide .env.example, Docker Compose development and production configurations, migrations, seed data, health checks, README documentation, and clear commands for running the entire project. The frontend must be lightweight, professional, fast, clean, compact, modern, premium, practical, and designed like a real professional SaaS business application rather than an AI-generated dashboard. Do not use excessive gradients, excessive glassmorphism, excessive shadows, giant rounded cards, unnecessary animations, decorative UI, fake statistics, meaningless charts, or excessive colors. Use a professional dark blue/charcoal theme as the default dark mode with background #0B1120, secondary background #111827, surface #172033, border #263248, primary text #F8FAFC, secondary text #94A3B8, primary accent #3B82F6, accent hover #2563EB, success #22C55E, warning #F59E0B, danger #EF4444, and info #38BDF8. Also provide a carefully designed light mode using background #F8FAFC, surface #FFFFFF, secondary surface #F1F5F9, border #E2E8F0, primary text #0F172A, secondary text #64748B, and primary #2563EB. Support Dark, Light, and System theme settings, default to Dark, and persist the user's preference. Use Inter or another professional modern sans-serif font. Use React Icons throughout the application and do not mix random icon libraries. Use a proper React dropdown menu component for user menus, table actions, property actions, bulk actions, and settings. Build reusable UI components including Button, Input, Select, Dropdown, Modal, Drawer, Badge, Tooltip, Tabs, Table, Pagination, Search, Filter, Empty State, Error State, Loading State, Confirm Dialog, Image Gallery, Status Indicator, and Progress Indicator, and make all components theme-aware. Build professional reusable data tables with server-side pagination, server-side search, sorting, filtering, column visibility, row actions, bulk selection, bulk actions, loading states, empty states, error states, page-size selection, and pagination controls. Never load thousands of database records into React and paginate locally; pagination, search, filtering, and sorting must be connected to PostgreSQL through the Go API using query parameters such as page, page_size, search, sort, order, status, project_id, listing_type, and date filters. Build authentication with secure password hashing, login, logout, protected routes, authentication state, secure session/token handling, role-based authorization, user profile, and roles ADMIN, AGENT, EDITOR, and VIEWER. Build the property management module with properties, property sources, property details, status workflow, unique property code generation, create, edit, archive, detail view, list view, search, filters, sorting, server-side pagination, bulk actions, and property processing. The property workflow statuses must include NEW, IMPORTING, IMPORTED, PROCESSING, CONTENT_READY, IMAGES_READY, READY_FOR_REVIEW, APPROVED, REJECTED, SCHEDULED, PUBLISHING, PUBLISHED, FAILED, and ARCHIVED. The main property collection screen must be extremely simple: Facebook Post URL input and Add Property button. When the user enters a Facebook URL, create the property with a unique backend-generated property code such as BH260816-0001 and status NEW. Do not automatically start processing unless configured to do so; provide a Process Property action so the user can collect multiple URLs first and process them later. Build a Property Inbox showing property code, project, price, listing type, source, status, created date, and actions. Build property database entities for properties, property_sources, property_details, projects, project_assets, property_images, image_processing_jobs, prompt_templates, prompt_versions, ai_generations, content_versions, watermarks, publishing_jobs, social_accounts, social_posts, google_sheet_integrations, automation_settings, notifications, activity_logs, processing_logs, users, roles, permissions, and system_settings. Property fields should support property code, project, property type, listing type, title, description, rent price, sale price, bedrooms, bathrooms, size in square meters, floor, total floors, furnishing, parking, pet policy, availability date, address, district, province, location, BTS, MRT, distance, owner, agent, contact, Line ID, phone, source, and source URL; do not force every field to be required because source listings may not contain every detail. Store original Facebook source information separately, including source type, source URL, source post ID where available, source author, source description, original timestamp, and imported timestamp. Never destroy original source data. For Facebook content and image collection, build an importer abstraction that can attempt supported automatic import where available but must provide a manual fallback for content and image upload if automatic import is unavailable; do not assume unsupported Facebook scraping functionality. Never make the entire application dependent on arbitrary Facebook scraping. Build a Project/Condo Management module where each project represents a condo/building and can contain reusable project assets such as 1.jpg, 2.jpg, 3.jpg, etc. Store project assets in object storage, not PostgreSQL binary fields. Support project create, edit, list, search, detail, asset upload, sorting, deletion, active/inactive status, preview, and configurable number of project images used before property images. Default Facebook project images should be 2, so final Facebook image order should be project image 1, project image 2, property image 1, property image 2, property image 3, and so on. Build complete property image management with multiple uploads, sorting, preview, deletion, replacement, metadata, status, and processing. Image states must include ORIGINAL, PROCESSING, ENHANCED, WATERMARKED, FINAL, and FAILED. Store property ID, original filename, object-storage path, MIME type, width, height, size, sort order, processing status, and timestamps. Never overwrite original images. Organize the logical asset lifecycle as original → enhanced → watermarked → final. Use asynchronous background jobs for image processing rather than blocking HTTP requests. Create an image-processing provider abstraction supporting MOCK IMAGE PROVIDER for local development and REAL IMAGE PROVIDER for production. Build a reusable watermark system that supports logo, position, opacity, size, and margin, with positions top-left, top-right, bottom-left, bottom-right, and center; watermarking must be deterministic and must never modify original images. Build a Prompt Template Manager so AI prompts are never hard-coded into business logic. Support templates such as Facebook Rental, Facebook Sale, TikTok, English, Thai, Google Sheet, and Image Enhancement. Each prompt must have versions, variables, active status, model information, and timestamps. Only one prompt version should be active at a time. Store every AI generation with property ID, prompt version, model, input, output, status, errors, token/cost metadata where available, and timestamp. Support MOCK AI PROVIDER for local development and REAL AI PROVIDER for production. Build content versioning so Facebook Content V1, V2, V3 and TikTok Content V1, V2, V3 can exist without overwriting previous versions. Generate platform-specific content rather than blindly reusing Facebook content for TikTok. Facebook content should support title, description, property details, price, location, CTA, and hashtags; TikTok content should support hook, property highlights, price, location, CTA, and hashtags. Build an AI image-enhancement workflow where original images are queued, processed, enhanced, watermarked, and converted into final images, with each job storing job ID, property ID, image ID, status, retry count, start time, completion time, and error message. Build a complete validation engine that checks project information, property information, price where required, property type, required images, minimum image count, project assets, AI content, watermark, processing completion, required contact information, and publishing readiness. Validation results must be PASS, WARNING, or ERROR and must be clearly displayed to the user. Build a dedicated Review Center as one of the most important parts of the application. The Review Center must show property information, source information, original images, enhanced images, final images, project images, Facebook content, TikTok content, validation results, processing status, and publishing configuration. Provide Edit, Save, Regenerate, Approve, Reject, Retry, and Preview actions. Default approval_required to TRUE. If approval is required, the system must never publish until the user explicitly approves the property. Build three automation modes: Manual, Semi-Automatic, and Automatic, with Semi-Automatic as the default. Manual mode requires user actions; Semi-Automatic automatically processes the property but requires approval before publishing; Automatic processes, validates, and publishes automatically when all validation rules pass. Build Automation Settings allowing the user to configure auto import, auto content generation, auto image processing, auto watermark, auto validation, approval required, auto Facebook publishing, auto TikTok publishing, and Google Sheets synchronization. Record automation setting changes in activity logs and display warnings before enabling automatic publishing. Build Redis/Asynq background queues for property-import, ai-content, ai-image, watermark, validation, google-sheet, facebook-publish, and tiktok-publish. Every background job must support status, retry, timeout, logging, error handling, idempotency, and manual retry from the UI. Create a processing monitor showing exactly what the application is doing, such as Import 100%, Content 100%, Images 70%, Watermark Waiting, Validation Waiting. The property processing pipeline should be NEW → IMPORTING → IMPORTED → PROCESSING → CONTENT_READY → IMAGES_READY → READY_FOR_REVIEW. Build an activity log that records events such as property creation, source import, AI generation, image processing, watermark completion, validation, approval, publishing, and failures, including timestamps, user, property ID, job ID, and relevant details. Never silently fail; every error must be visible, logged, retryable, and understandable. Preserve successful previous results when later jobs fail. Build Google Sheets as an integration/output layer only; PostgreSQL must always be the source of truth. Support Google account connection architecture, spreadsheet configuration, sheet selection, column mapping, manual sync, automatic sync, sync status, sync logs, and retry. Use a Google Sheets provider abstraction and a mock provider for development without credentials. Build social publishing using a generic SocialPlatformProvider architecture with independent Facebook and TikTok providers. Implement account connection, authentication, publishing, scheduling where officially supported, status tracking, external post ID, external URL, errors, retry, and publishing history. Use official APIs and required permissions only and never assume unsupported functionality. If an external operation is not available through an official supported API, provide a manual publishing fallback in the UI rather than pretending the integration works. Facebook and TikTok must be isolated providers so failure on one platform does not affect the other. Build Publishing Queue with QUEUED, PROCESSING, SUCCESS, FAILED, and CANCELLED statuses, and connect APPROVED → QUEUED → PUBLISHING → SUCCESS/FAILED. Build the dashboard around operational usefulness rather than meaningless analytics. Show New Properties, Processing, Ready for Review, Approved, Publishing, Published, Failed, recent properties, processing queue, failed jobs, approval queue, and publishing queue. The dashboard should answer "What do I need to do next?" Build a professional Property Detail page with Overview, Property Information, Source, Images, AI Content, Social Content, Processing, Publishing, and Activity sections. Build search, filtering, sorting, pagination, and bulk operations throughout the property system. Add filters for status, project, listing type, source, date, price, and property type. Build responsive design for desktop, laptop, tablet, and mobile, with desktop as the primary experience. Mobile must still allow property review, approval, status checking, basic editing, and publishing control. Use lazy loading, caching, image thumbnails, responsive image sizes, background jobs, database indexes, and minimal dependencies to keep the application lightweight. Create PostgreSQL indexes for property code, status, project ID, listing type, created_at, source URL, price, and property type, and use PostgreSQL full-text search or suitable indexed search where useful. Build secure authentication and security controls including input validation, SQL injection protection through GORM/parameterized queries, authorization, rate limiting, secure password hashing, CORS, CSRF protection where applicable, secure file upload validation, MIME validation, maximum upload size, safe filenames, environment variables for secrets, and never expose API keys in the React frontend. Use feature-based frontend architecture with src/app, components, features/auth, features/properties, features/projects, features/images, features/ai, features/prompts, features/publishing, features/integrations, features/settings, layouts, pages, hooks, lib, services, types, and utils. Use reusable frontend components and avoid putting the entire application into one giant components directory. Use REST APIs organized by /api/auth, /api/users, /api/properties, /api/projects, /api/images, /api/prompts, /api/ai, /api/processing, /api/publishing, /api/social, /api/google-sheets, /api/settings, and /api/logs, with consistent HTTP status codes, validation, pagination, filtering, sorting, authentication, and error formats. API success responses should consistently contain data and meta where appropriate; error responses should contain error, code, message, and details without exposing internal stack traces. Use PostgreSQL migrations for every schema change and never manually modify production tables. Create development seed data including an admin user, sample projects, sample properties, sample prompt templates, processing logs, and publishing records, but never use fake data in production. Implement backend unit tests, service tests, repository tests, API tests, frontend component and validation tests, and end-to-end workflow tests covering Create Property → Process → Generate Content → Process Images → Validate → Approve → Queue Publishing. Add structured logging with request ID, user ID, property ID, job ID, service, error, and execution time. Use Docker Desktop as the required local development environment. Create docker-compose.yml, docker-compose.dev.yml, and docker-compose.prod.yml with frontend, api, worker, postgres, redis, and nginx where appropriate. Development should support hot reload where practical. Use .env and .env.example and never commit secrets. The project must be runnable by starting Docker Desktop and then running the documented Docker Compose command, with the frontend, Go API, PostgreSQL, Redis, and worker communicating correctly. Create a professional README explaining prerequisites, Docker Desktop setup, environment configuration, migrations, seed data, development commands, testing, production build, and troubleshooting. Use Git-friendly architecture and meaningful commits such as feat: initialize project architecture, feat: add authentication, feat: add property management, feat: add project management, feat: add image pipeline, feat: add AI content generation, feat: add review workflow, feat: add publishing queue, and fix: descriptions. Build the project phase by phase in this exact order: Phase 1 project initialization and Docker, Phase 2 authentication and authorization, Phase 3 property management, Phase 4 project/condo management, Phase 5 image management and processing, Phase 6 prompt management and AI content pipeline, Phase 7 watermark and validation engine, Phase 8 Review Center and human approval, Phase 9 Redis/Asynq background automation pipeline, Phase 10 Google Sheets integration, Phase 11 Facebook and TikTok publishing integrations, Phase 12 automation settings, Phase 13 production quality, testing, security, performance, and UI polish. Do not attempt to build all phases simultaneously. Complete one phase, run the application, run tests, check migrations, check Docker containers, check frontend build, check backend API, fix all errors, and only then proceed to the next phase. At every stage, maintain the established design system and architecture. Before considering any page complete, evaluate whether the primary action is obvious, whether the user can understand the page quickly, whether information hierarchy is clear, whether tables are easy to scan, whether statuses are obvious, whether errors are understandable, whether the interface is too crowded, whether unnecessary decoration has been removed, whether dark mode looks intentional, whether light mode looks intentional, whether mobile is usable, and whether loading, empty, error, and confirmation states exist. The final user workflow must be: manually find an interesting property on Facebook → copy Facebook post URL → paste URL into the application → save property → process property → import or manually provide source content/images → structure property information → generate Facebook content → generate TikTok content → enhance images → watermark images → automatically add project/condo images → validate → review → approve or reject → queue publishing → publish to Facebook → publish to TikTok → synchronize to Google Sheets → track published URLs and statuses → monitor errors and retry failed jobs. PostgreSQL must be the single source of truth, object storage must hold media, Redis must handle background jobs, Go must own business logic and integrations, React must provide the professional operational interface, and the system must remain lightweight, maintainable, secure, scalable, and ready to evolve into a multi-agent real-estate SaaS platform. Do not generate unnecessary code before the current phase is clearly defined and working. Do not skip validation, error handling, migrations, Docker verification, testing, or responsive UI. Do not use fake integrations. When credentials are required, create proper provider abstractions and development mocks. Always keep the human approval gate safe and explicit. The final product should reduce the user's current manual workflow of Facebook URL collection, copy/paste into ChatGPT, individual image enhancement, downloading, watermarking, copying to Google Sheets, preparing Facebook Business Suite posts, and preparing TikTok posts into one centralized professional workflow where the user mainly collects the Facebook URL, reviews the generated result, approves it, and lets the system handle the remaining repeatable processes.
